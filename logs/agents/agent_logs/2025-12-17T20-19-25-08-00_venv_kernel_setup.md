# venv + Jupyter kernel setup (kaggle_clicks)

- Timestamp: 2025-12-17T20:19:25-08:00
- Agent: GitHub Copilot
- Model: GPT-5.2

## What I did
- Created a repo-local virtualenv at `.venv/` using Python 3.10.14.
- Installed notebook/EDA dependencies into `.venv`:
  - `ipykernel`, `jupyter`, `pandas`, `numpy`, `matplotlib`, `seaborn`, `scipy`.
- Registered a Jupyter kernelspec:
  - Name: `kaggle_clicks`
  - Display name: `Python (kaggle_clicks .venv)`
  - Location: `/home/mykola/.local/share/jupyter/kernels/kaggle_clicks`
- Updated VS Code workspace settings to prefer the venv interpreter:
  - `.vscode/settings.json`: `python.defaultInterpreterPath = ${workspaceFolder}/.venv/bin/python`
- Fixed `human_src/eda.ipynb` imports to work without packaging metadata by adding repo-root detection + `sys.path` injection before `from kaggle_clicks.paths import get_paths`.

## Verification
- Confirmed `.venv` interpreter path: `/home/mykola/repos/kaggle_clicks/.venv/bin/python`.
- Confirmed kernelspec registered via `jupyter kernelspec list`.
- Re-ran notebook Cell 2 (imports) and Cell 3 (sample CSV load) successfully.

## Notes
- `pip install -e .` failed because the repo has no `pyproject.toml`/`setup.py`. Notebook import is handled via `sys.path` instead.
