# Combined Tables (TE vs no-TE)

This folder contains derived tables that directly compare the TE and no-TE 10% paper grids spec-by-spec.

## Files

- `te_vs_note_by_fold.csv`: paired per-fold metrics and deltas (TE − no-TE) for each `run_id` and fold.
- `te_vs_note_summary.csv`: fold-aggregated deltas (mean/std across folds) per `run_id`.
- `te_vs_note_summary.md`: markdown view of `te_vs_note_summary.csv`.

